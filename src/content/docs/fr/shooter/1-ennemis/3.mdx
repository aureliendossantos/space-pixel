---
type: tutorial
title: Les fonctions
---

<Video src="/videos/aurelien/fonctions.mp4" controls />

<Box icon="question-mark">

  ### Teste tes connaissances

  1. Les arguments peuvent être :

    <MultipleChoice>
      <Option message="Mauvaise réponse. Les arguments agissent comme des variables normales et peuvent être de tous les types.">Des nombres</Option>
      <Option message="Mauvaise réponse. Les arguments agissent comme des variables normales et peuvent être de tous les types.">Des booléens</Option>
      <Option isCorrect message="Bonne réponse. Les arguments agissent comme des variables normales et peuvent être de tous les types.">Tous les types de variable</Option>
    </MultipleChoice>

  2. La valeur de retour de la fonction est définie par l'instruction :

    <MultipleChoice>
      <Option message="Mauvaise réponse. 'retour' n'est pas une commande existante dans ce langage.">`retour`</Option>
      <Option isCorrect message="Bonne réponse. Ce qui suit le return sera renvoyé par la fonction quand elle est utilisée.">`return`</Option>
      <Option message="Mauvaise réponse. 'function' permet de déclarer une fonction, mais pas la valeur de retour.">`function`</Option>
    </MultipleChoice>
</Box>

<Box icon="check-list">

  ## Checklist

  <Checklist>
  - [ ] J'ai compris le principe des arguments.
  - [ ] J'ai compris qu'un argument est une variable locale à la fonction.
  - [ ] J'ai compris ce qu'est une valeur de retour.
  </Checklist>
</Box>
