---
type: tutorial
title: Afficher les étoiles au fond de l'écran
---

<Video src="/videos/flavie/afficher-etoiles.mp4" controls />

<Box icon="question-mark">

  ### Teste tes connaissances

  1. Comment sont déterminées les positions des étoiles ?

    <MultipleChoice>
      <Option message="Ce n’est pas la bonne réponse. La position des étoiles est déterminée de manière aléatoire à l'aide de la fonction rnd().">À l'aide de la fonction add()</Option>
      <Option message="Ce n’est pas la bonne réponse. La position des étoiles est déterminée de manière aléatoire à l'aide de la fonction rnd().">De manière aléatoire à l'aide de la fonction pset()</Option>
      <Option isCorrect message="C’est la bonne réponse. La position des étoiles est déterminée de manière aléatoire à l'aide de la fonction rnd().">De manière aléatoire à l'aide de la fonction rnd()</Option>
    </MultipleChoice>

  2. Comment ajouter une variable à un tableau ?

    <MultipleChoice>
      <Option isCorrect message="C’est la bonne réponse. Pour ajouter une variable à un tableau, on utilise la fonction add(). Cette fonction permet d'ajouter un élément à un tableau grace à la formule add (nom du tableau, nouvelle variable).">En utilisant la fonction add()</Option>
      <Option message="Ce n’est pas la bonne réponse. Pour ajouter une variable à un tableau, on utilise la fonction add(). Cette fonction permet d'ajouter un élément à un tableau grace à la formule add (nom du tableau, nouvelle variable).">En utilisant la fonction pset()</Option>
      <Option message="Ce n’est pas la bonne réponse. Pour ajouter une variable à un tableau, on utilise la fonction add(). Cette fonction permet d'ajouter un élément à un tableau grace à la formule add (nom du tableau, nouvelle variable).">En utilisant la fonction rnd()</Option>
    </MultipleChoice>

    3. Quelle est la première étape pour afficher des étoiles dans le jeu ?

    <MultipleChoice>
      <Option message="Ce n’est pas la bonne réponse. La première étape pour afficher des étoiles dans le jeu est de créer un tableau où sont rangées les étoiles. Cette étape est importante car elle permet de stocker les caractéristiques des étoiles (position, couleur, vitesse, etc.) dans un tableau, ce qui facilite leur manipulation ultérieure.">Déterminer la couleur des étoiles avec la fonction pset()</Option>
      <Option isCorrect message="C’est la bonne réponse. La première étape pour afficher des étoiles dans le jeu est de créer un tableau où sont rangées les étoiles. Cette étape est importante car elle permet de stocker les caractéristiques des étoiles (position, couleur, vitesse, etc.) dans un tableau, ce qui facilite leur manipulation ultérieure.">Créer un tableau où sont rangées les étoiles</Option>
      <Option message="Ce n’est pas la bonne réponse. La première étape pour afficher des étoiles dans le jeu est de créer un tableau où sont rangées les étoiles. Cette étape est importante car elle permet de stocker les caractéristiques des étoiles (position, couleur, vitesse, etc.) dans un tableau, ce qui facilite leur manipulation ultérieure.">Créer une boucle for avec un compteur allant de 1 à 20</Option>
    </MultipleChoice>
</Box>

<Box icon="check-list">

  ## Checklist

  <Checklist>
  - [ ] J'ai réussi à afficher un pixel de couleur avec la fonction pset().
  - [ ] J'ai réussi à utiliser la fonction rnd() pour positionner le pixel à un endroit aléatoire.
  </Checklist>
</Box>
