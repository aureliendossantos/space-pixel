---
type: tutorial
title: Animer les étoiles
---

<Video src="/videos/flavie/animer-etoiles.mp4" controls />

<Box icon="question-mark">

  ### Teste tes connaissances

  1. Comment peut-on animer les étoiles en utilisant une boucle for ?

    <MultipleChoice>
      <Option isCorrect message="C’est la bonne réponse. Pour animer les étoiles, on utilise une boucle for. Pour cela, nous devons parcourir chaque étoile dans le tableau des étoiles avec une boucle for et mettre à jour leur coordonnée Y. Nous pouvons redéfinir la position Y de chaque étoile en ajoutant une valeur à leur position actuelle.">En mettant à jour les coordonnées Y de chaque étoile dans la boucle for</Option>
      <Option message="Ce n’est pas la bonne réponse. Pour animer les étoiles, on utilise une boucle for. Pour cela, nous devons parcourir chaque étoile dans le tableau des étoiles avec une boucle for et mettre à jour leur coordonnée Y. Nous pouvons redéfinir la position Y de chaque étoile en ajoutant une valeur à leur position actuelle.">En définissant une fonction UPDATE pour mettre à jour les coordonnées des étoiles</Option>
      <Option message="Ce n’est pas la bonne réponse. Pour animer les étoiles, on utilise une boucle for. Pour cela, nous devons parcourir chaque étoile dans le tableau des étoiles avec une boucle for et mettre à jour leur coordonnée Y. Nous pouvons redéfinir la position Y de chaque étoile en ajoutant une valeur à leur position actuelle.">En utilisant une fonction RND pour définir des coordonnées aléatoires pour chaque étoile dans la boucle for</Option>
    </MultipleChoice>

  2. Dans quelle fonction met-on à jour la position Y de chaque étoile ?

    <MultipleChoice>
      <Option message="Ce n’est pas la bonne réponse. La fonction utilisée dans le script pour mettre à jour la position Y de chaque étoile est la fonction _update().">pset()</Option>
      <Option isCorrect message="C’est la bonne réponse. La fonction utilisée dans le script pour mettre à jour la position Y de chaque étoile est la fonction _update().">_update()</Option>
      <Option message="Ce n’est pas la bonne réponse. La fonction utilisée dans le script pour mettre à jour la position Y de chaque étoile est la fonction _update().">add()</Option>
    </MultipleChoice>
</Box>

<Box icon="check-list">

  ## Checklist

  <Checklist>
  - [ ] J'ai utilisé une condition pour faire réapparaître les étoiles en haut de l'écran.
  - [ ] J'ai replacé les étoiles dans une position aléatoire grâce à rnd().
  </Checklist>
</Box>
