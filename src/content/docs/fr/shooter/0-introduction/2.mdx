---
type: tutorial
title: Les types de variable
---

<Video src="/videos/roxana/capsule-variables.mp4" controls />

<Box icon="question-mark">

  ### Teste tes connaissances

  1. Comment déclare-t-on une chaîne de caractères ?

    <MultipleChoice>
      <Option message="Ce n’est pas la bonne réponse, on déclare une chaîne de caractères avec des guillemets. Il est important de respecter cela car les guillemets sont utilisés pour différencier les chaînes de caractères des autres types de variables dans Pico-8.">Avec des accolades \{ }</Option>
      <Option message="Ce n’est pas la bonne réponse, on déclare une chaîne de caractères avec des guillemets. Il est important de respecter cela car les guillemets sont utilisés pour différencier les chaînes de caractères des autres types de variables dans Pico-8.">Avec des crochets [ ]</Option>
      <Option isCorrect message="C’est la bonne réponse, on déclare une chaîne de caractères avec des guillemets. Il est important de respecter cela car les guillemets sont utilisés pour différencier les chaînes de caractères des autres types de variables dans Pico-8.">Avec des guillemets " "</Option>
    </MultipleChoice>

  2. Quel est le type de variable utilisé pour stocker des nombres entiers ?

    <MultipleChoice>
      <Option isCorrect message="C’est la bonne réponse, en Pico-8, le type de variable utilisé pour stocker des nombres entiers est numérique. Les tableaux peuvent stocker des collections de valeurs différentes et les booléens peuvent stocker deux valeurs différentes : vrai ou faux.">Numérique</Option>
      <Option message="Ce n’est pas la bonne réponse, en Pico-8, le type de variable utilisé pour stocker des nombres entiers est numérique. Les tableaux peuvent stocker des collections de valeurs différentes et les booléens peuvent stocker deux valeurs différentes : vrai ou faux.">Tableaux</Option>
      <Option message="Ce n’est pas la bonne réponse, en Pico-8, le type de variable utilisé pour stocker des nombres entiers est numérique. Les tableaux peuvent stocker des collections de valeurs différentes et les booléens peuvent stocker deux valeurs différentes : vrai ou faux.
">Booléens</Option>
    </MultipleChoice>
    
  3. Quelle est la différence entre une variable locale et une variable globale ?

    <MultipleChoice>
      <Option message="Ce n’est pas la bonne réponse, la variable locale est déclarée à l'intérieur d'une fonction et n'est accessible que dans celle-ci, tandis que la variable globale est déclarée à l'extérieur de toutes les fonctions et peut être utilisée dans toutes les fonctions. ">Les variables locales sont accessibles partout dans le programme, tandis que les variables globales ne peuvent être utilisées que dans la fonction où elles sont déclarées.</Option>
      <Option message="Ce n’est pas la bonne réponse, la variable locale est déclarée à l'intérieur d'une fonction et n'est accessible que dans celle-ci, tandis que la variable globale est déclarée à l'extérieur de toutes les fonctions et peut être utilisée dans toutes les fonctions.">Les variables globales sont déclarées à l'intérieur des fonctions, tandis que les variables locales peuvent être déclarées n'importe où.</Option>
      <Option isCorrect message="C'est la bonne réponse.">Les variables locales sont accessibles uniquement dans la fonction où elles sont déclarées, tandis que les variables globales peuvent être utilisées dans toutes les fonctions du programme.</Option>
    </MultipleChoice>
</Box>

<Box icon="check-list">

  ## Checklist

  <Checklist>
  - [ ] J'ai compris le rôle de chaque variable.
  - [ ] J'ai compris la différence entre variables locales et globales.
  </Checklist>
</Box>
